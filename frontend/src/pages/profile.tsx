import AcoountSetup from "./acoountSetup"
import SupplierSetup from "./supplierSetup"

const profile = () => {
  return (
    <div>

        <main>
            <AcoountSetup />
            <SupplierSetup />
        </main>
    </div>
  )
}

export default profile